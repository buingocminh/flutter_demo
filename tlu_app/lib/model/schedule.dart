class Schedule {
  final id;
  final name;
  final room;
  final time;
  final startDate;
  final endDate;
  final teacher;
  Schedule({this.id,this.name,this.room, this.time, this.startDate, this.endDate, this.teacher });
}